{
  "name": "sipper-extension",
  "version": "0.1.0",
  "private": true,
  "description": "Chrome extension that adds SIP accounts from a PBX web UI to Sipper",
  "type": "module",
  "scripts": {
    "test": "node --test \"tests/*.test.mjs\""
  },
  "devDependencies": {
    "jsdom": "^30.0.1"
  }
}
