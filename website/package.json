{
  "name": "sipper-website",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "check": "wrangler deploy --dry-run",
    "test": "node --test \"test/*.test.js\"",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.131.2"
  }
}
